% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{TDVS_j}
\alias{TDVS_j}
\title{TDVS variable selection for a single predictor j}
\usage{
TDVS_j(
  test_index,
  dataXY,
  init_beta_TDVS = rep(1, 8),
  B = 300,
  sig_cutoff = 0.05,
  init_beta0_TDVS = 1,
  init_nu_TDVS = 1,
  init_gamma_TDVS = 1,
  init_theta_TDVS = 0.5,
  SS_t0_TDVS = 10,
  SS_t1_TDVS = 1,
  hyper_mu_beta0_TDVS = 0,
  hyper_sigma_beta0_TDVS = 1e+06,
  hyper_mu_nu_TDVS = 1,
  hyper_sigma_nu_TDVS = 1,
  hyper_c_gamma_TDVS = 1e-04,
  hyper_d_gamma_TDVS = 1e-04,
  hyper_a_theta_TDVS = 1,
  hyper_b_theta_TDVS = -1,
  max_iter_TDVS = 100,
  tol_TDVS = 1e-06,
  add_correc_CiS = 0.001,
  beta_method = c("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik")
)
}
\arguments{
\item{test_index}{Index of predictor to test}

\item{dataXY}{List containing data X and Y}

\item{init_beta_TDVS}{Initial vector for beta (default: rep(1, number of predictors))}

\item{B}{Number of iterations (default: 300)}

\item{sig_cutoff}{Threshold used for significance cutoff in variable selection (default: 0.05)}

\item{init_beta0_TDVS}{Initial value for beta0 (default: 1)}

\item{init_nu_TDVS}{Initial value for nu (default: 1)}

\item{init_gamma_TDVS}{Initial value for gamma (default: 1)}

\item{init_theta_TDVS}{Initial value for theta (default: 0.5)}

\item{SS_t0_TDVS}{the "spike" hyperparameter in beta's spike-and-slab prior (default: 10)}

\item{SS_t1_TDVS}{the "slab" hyperparameter in beta's spike-and-slab prior (default: 1)}

\item{hyper_mu_beta0_TDVS}{the mean hyperparameter in beta0's Normal prior (default: 0)}

\item{hyper_sigma_beta0_TDVS}{the variance hyperparameter in beta0's Normal prior (default: 1e6)}

\item{hyper_mu_nu_TDVS}{the location hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_sigma_nu_TDVS}{the scale hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_c_gamma_TDVS}{the shape hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_d_gamma_TDVS}{the rate hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_a_theta_TDVS}{the shape hyperparameter a in theta's beta prior (default: 1)}

\item{hyper_b_theta_TDVS}{the shape hyperparameter b in theta's beta prior (default: the number of predictors)}

\item{max_iter_TDVS}{Maximum number of iterations (default: 100)}

\item{tol_TDVS}{Convergence tolerance (default: 1e-6)}

\item{add_correc_CiS}{Correction factor for CiS (default: 1e-3)}

\item{beta_method}{Optimization method for beta updates ("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik"). Defaults to "nlm".}
}
\value{
List containing variable selection results
}
\description{
TDVS variable selection for a single predictor j
}
