% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{per_fun}
\alias{per_fun}
\title{Calculate permutation value of CiS for a single index}
\usage{
per_fun(
  j_index,
  dataXY,
  init_beta_per = rep(1, 8),
  init_beta0_per = 1,
  init_nu_per = 1,
  init_gamma_per = 1,
  init_theta_per = 0.5,
  SS_t0_per = 10,
  SS_t1_per = 1,
  hyper_mu_beta0_per = 0,
  hyper_sigma_beta0_per = 1e+06,
  hyper_mu_nu_per = 1,
  hyper_sigma_nu_per = 1,
  hyper_c_gamma_per = 1e-04,
  hyper_d_gamma_per = 1e-04,
  hyper_a_theta_per = 1,
  hyper_b_theta_per = -1,
  max_iter_per = 100,
  tol_per = 1e-06,
  add_correc_CiS = 0.001,
  beta_method = c("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik")
)
}
\arguments{
\item{j_index}{Index of predictor for permutation}

\item{dataXY}{List containing data X and Y}

\item{init_beta_per}{Initial vector for beta (default: rep(1, number of predictors))}

\item{init_beta0_per}{Initial value for beta0 (default: 1)}

\item{init_nu_per}{Initial value for nu (default: 1)}

\item{init_gamma_per}{Initial value for gamma (default: 1)}

\item{init_theta_per}{Initial value for theta (default: 0.5)}

\item{SS_t0_per}{the "spike" hyperparameter in beta's spike-and-slab prior (default: 10)}

\item{SS_t1_per}{the "slab" hyperparameter in beta's spike-and-slab prior (default: 1)}

\item{hyper_mu_beta0_per}{the mean hyperparameter in beta0's Normal prior (default: 0)}

\item{hyper_sigma_beta0_per}{the variance hyperparameter in beta0's Normal prior (default: 1e6)}

\item{hyper_mu_nu_per}{the location hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_sigma_nu_per}{the scale hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_c_gamma_per}{the shape hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_d_gamma_per}{the rate hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_a_theta_per}{the shape hyperparameter a in theta's beta prior (default: 1)}

\item{hyper_b_theta_per}{the shape hyperparameter b in theta's beta prior (default: the number of predictors)}

\item{max_iter_per}{Maximum number of iterations (default: 100)}

\item{tol_per}{Convergence tolerance (default: 1e-6)}

\item{add_correc_CiS}{Correction factor for CiS (default: 1e-3)}

\item{beta_method}{Optimization method for beta updates ("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik"). Defaults to "nlm".}
}
\value{
Permutation function value
}
\description{
Calculate permutation value of CiS for a single index
}
