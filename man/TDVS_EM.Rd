% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{TDVS_EM}
\alias{TDVS_EM}
\title{EM algorithm for TDVS}
\usage{
TDVS_EM(
  dataXY,
  init_beta = rep(1, 8),
  init_beta0 = 1,
  init_nu = 1,
  init_gamma = 1,
  init_theta = 0.5,
  SS_t0 = 10,
  SS_t1 = 1,
  hyper_mu_beta0 = 0,
  hyper_sigma_beta0 = 1e+06,
  hyper_mu_nu = 1,
  hyper_sigma_nu = 1,
  hyper_c_gamma = 1e-04,
  hyper_d_gamma = 1e-04,
  hyper_a_theta = 1,
  hyper_b_theta = -1,
  max_iter = 100,
  tol = 1e-06,
  conv_type = "param",
  beta_method = c("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik")
)
}
\arguments{
\item{dataXY}{List containing data X and Y}

\item{init_beta}{Initial vector for beta (default: rep(1, number of predictors))}

\item{init_beta0}{Initial value for beta0 (default: 1)}

\item{init_nu}{Initial value for nu (default: 1)}

\item{init_gamma}{Initial value for gamma (default: 1)}

\item{init_theta}{Initial value for theta (default: 0.5)}

\item{SS_t0}{the "spike" hyperparameter in beta's spike-and-slab prior (default: 10)}

\item{SS_t1}{the "slab" hyperparameter in beta's spike-and-slab prior (default: 1)}

\item{hyper_mu_beta0}{the mean hyperparameter in beta0's Normal prior (default: 0)}

\item{hyper_sigma_beta0}{the variance hyperparameter in beta0's Normal prior (default: 1e6)}

\item{hyper_mu_nu}{the location hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_sigma_nu}{the scale hyperparameter in nu's log-normal prior (default: 1)}

\item{hyper_c_gamma}{the shape hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_d_gamma}{the rate hyperparameter in gamma's gamma prior (default: 1e-4)}

\item{hyper_a_theta}{the shape hyperparameter a in theta's beta prior (default: 1)}

\item{hyper_b_theta}{the shape hyperparameter b in theta's beta prior (default: the number of predictors)}

\item{max_iter}{Maximum number of iterations (default: 100)}

\item{tol}{Convergence tolerance (default: 1e-6)}

\item{conv_type}{Convergence type (default: "param")}

\item{beta_method}{Optimization method for beta updates ("nlm", "optim", "maxLik", "cd_nlm", "cd_maxLik"). Defaults to "nlm".}
}
\value{
List of results from the EM algorithm
}
\description{
EM algorithm for TDVS
}
